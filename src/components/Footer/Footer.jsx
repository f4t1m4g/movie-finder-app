import { Layout } from "antd";
import "./Footer.sass";

const Footer = () => {
  const { Footer } = Layout;

  return (
    <Footer className="footer">
      <p>Hecho por Fatima con mucho ❤</p>
    </Footer>
  );
};

export default Footer;